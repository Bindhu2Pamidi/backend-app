spring:
  application:
    name: app
  datasource:
    url: jdbc:postgresql://postgres-db:5432/postgres
    username: keerthanapamidi
    password: Krishna@26
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
